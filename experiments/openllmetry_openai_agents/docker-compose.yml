services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-obs-openllmetry-openai-agents
    env_file: .env
    ports:
      - "8004:8004"
    environment:
      PORT: "8004"
    extra_hosts:
      - "host.docker.internal:host-gateway"
